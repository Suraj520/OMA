<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/toggle_depth_color"
        android:icon="@drawable/ic_depth_color"
        android:title="@string/toggle_depth_color"
        app:showAsAction="always"/>

    <item
        android:id="@+id/toggle_mask"
        android:icon="@drawable/ic_mask_off"
        android:title="@string/toggle_mask"
        app:showAsAction="always"/>

    <item
        android:id="@+id/toggle_quantize"
        android:icon="@drawable/ic_quantize"
        android:title="@string/quantizeMask"
        app:showAsAction="always"/>

    <item
        android:id="@+id/reset"
        android:icon="@drawable/ic_reset"
        android:title="@string/reset"
        app:showAsAction="always"/>

</menu>